[package]
name = "clinkz-wot-binding-core"
version.workspace = true
edition.workspace = true

[dependencies]
clinkz-wot-td = { path = "../clinkz-wot-td", default-features = false }

[features]
default = ["std"]
std = ["clinkz-wot-td/std"]
