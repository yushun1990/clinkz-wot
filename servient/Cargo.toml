[package]
name = "clinkz-wot-servient"
version.workspace = true
edition.workspace = true

[dependencies]
clinkz-wot-core = { path = "../core" }
clinkz-wot-discovery = { path = "../discovery" }
clinkz-wot-protocol-bindings = { path = "../protocol-bindings/core" }
clinkz-wot-td = { path = "../td" }

[features]
default = ["std"]
std = []
