schema_version = 1
design_revision = "4.9"
work_package = "WP-100"
tranche = "WP-100-HANDLER-CONTEXT"
implementation_ref = "ae51858d82e8c1b9f621798674155015820c0535"
recorded_on = "2026-07-26"
verification_command = "tools/check-wp100-handler-context.sh"
verification_check = "wp100-handler-context-check"
status = "passed"
evidence_key = "handler-context"

requirement_ids = [
    "API-SURFACE-001",
    "HANDLER-API-001",
]
compilation_cells = ["no-default", "async-no-std", "std"]
coverage = [
    "exact actual-source AST projection for HandlerContext",
    "Core root and handler-module exports in every registered feature cell",
    "all 72 operation and target-kind pairings with exactly 18 accepted",
    "Validation/Validate/Never mapping for all 54 incompatible pairings",
    "fixed-size Thing slot, operation, plan, and optional binding error context",
    "private fields and negative Hash and Default compile contracts",
    "copyable borrowed Thing and affordance identity with const getters",
    "Core unit and downstream Servient and Protocol Binding compilation",
    "handler-value-primitives predecessor regression suite",
]
