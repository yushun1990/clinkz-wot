[package]
name = "validated-thing-decode-boundary-probe"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
clinkz-wot-td = { path = "../../../td" }
clinkz-wot-foundation = { path = "../../../foundation" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
time = { version = "0.3", default-features = false, features = ["alloc"] }
