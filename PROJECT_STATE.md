# Project State

> This file is the AI-maintained durable continuation checkpoint for
> `clinkz-wot`.
>
> It is intentionally not initialized by a human-authored fixed template.
> The first substantial agent must reconstruct the current state of the project,
> choose a structure that best supports rapid and correct continuation, and
> replace this bootstrap text.
>
> Follow the continuity, validation, and checkpoint rules in `AGENTS.md`.

## Initialization Status

UNINITIALIZED

Before beginning new substantial work, reconstruct and record the current
project state from the smallest sufficient set of authoritative sources,
including as applicable:

- `PLAN.md`;
- `docs/architecture/README.md`;
- accepted ADRs;
- `docs/artifacts.csv`;
- `docs/work-packages/index.toml`;
- active `workspace/` topics;
- relevant implementation and tests;
- current audits and evidence.

The initialized state must be useful to a fresh agent that has no access to
previous conversations. It should preserve the project understanding and
execution context that would otherwise have to be rediscovered.

After initialization, remove this bootstrap section and maintain the file
continuously.
