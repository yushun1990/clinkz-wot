schema_version = 1
design_revision = "5.0"
work_package = "WP-200"
tranche = "WP-200-PROPERTY-READ-ROUTE-RESERVATION-PROJECTION"
implementation_ref = "b47899150aa957b1dea8d844aa49852e3e6aa356"
recorded_on = "2026-08-10"
verification_command = "tools/check-wp200-property-read-route-reservation.sh"
verification_check = "wp200-property-read-route-reservation-check"
status = "passed"
evidence_key = "property-read-route-reservation-projection"

requirement_ids = [
    "PLAN-SET-001",
    "PLAN-ARTIFACT-001",
    "FORM-OWNER-001",
    "BIND-REG-001",
    "BIND-ROUTE-001",
]
compilation_cells = ["no-default", "async-no-std", "std"]
coverage = [
    "exact five-path pre-source checkpoint and immediate three-path implementation topology",
    "concrete binding compiler creates deterministic canonical mock route identity from resolved route and immutable binding inputs",
    "Producer-route artifacts require reservation metadata while Consumer artifacts reject it and return the complete original artifact",
    "typed and Core-owned host-erased compiler paths preserve the reservation without exposing opaque payload interpretation",
    "Planning retains the admitted envelope metadata without constructing collision domains or endpoint keys",
    "real compiler output reaches the admitted envelope, BindingRouteKey, and WP-300 route preparation without runner-owned identity constants",
    "no-default, async-no-std, and std compilation plus Core and Planning tests",
    "WP-200 plan, WP-300 binding, and Producer-route predecessor regressions plus absence of premature WP-400 architecture fixtures",
]
