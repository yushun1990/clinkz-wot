schema_version = 1
target_design_revision = "5.0"
status = "active-reviewed"
decision = "docs/ADRs/0018-bounded-v5-normative-authority-reset.org"
rule = "Only active_claim_ids may satisfy v5 gates; inactive_claim_ids remain historical implementation facts."

[[record]]
source = "docs/evidence/WP-000.toml"
verification_command = "tools/check-wp-000.sh"
active_claim_ids = [
  "PROFILE-AXIS-001",
  "FEATURE-MATRIX-001",
  "CRATE-DEPS-001",
  "RES-LIMIT-001",
  "RES-LIMIT-002",
  "RES-LIMIT-003",
  "API-RESOURCE-001",
  "ADMIT-TXN-001",
  "ADMIT-MEM-001",
  "API-SOURCE-TIME-001",
  "API-TYPES-001",
  "TIME-001",
  "CONSTRAINED-STORAGE-001",
]
inactive_claim_ids = [
  "API-SURFACE-001",
  "RES-LIMIT-004",
  "RES-PROFILE-001",
  "HOST-DEFAULT-001",
  "HOST-DEFAULT-002",
  "PERF-ACCOUNT-001",
]
disposition = "Reaffirm active claims with the unchanged checker; inactive claims cannot close a v5 gate. Logical-time claims are superseded by the later corrective evidence below."

[[record]]
source = "docs/evidence/WP-100-foundation-refresh.toml"
verification_command = "tools/check-wp100-foundation-refresh.sh"
active_claim_ids = [
  "API-RESOURCE-001",
  "CONSTRAINED-PROGRESS-001",
  "CONSTRAINED-STORAGE-002",
  "CONSTRAINED-WORK-001",
  "RES-LIMIT-001",
  "RES-LIMIT-002",
]
inactive_claim_ids = ["API-SURFACE-001", "RES-PROFILE-001"]
disposition = "Reaffirm active implementation facts; retain inactive profile/surface checks as regression input only."

[[record]]
source = "docs/evidence/WP-100-handler-value-primitives.toml"
verification_command = "tools/check-wp100-handler-value-primitives.sh"
active_claim_ids = ["HANDLER-VALUE-001"]
inactive_claim_ids = ["API-SURFACE-001"]
disposition = "Reaffirm the five active handler values; the retired umbrella surface id carries no gate authority."

[[record]]
source = "docs/evidence/WP-100-logical-time-correction.toml"
verification_command = "tools/check-wp100-logical-time-correction.sh"
active_claim_ids = [
  "API-SOURCE-TIME-001",
  "API-TYPES-001",
  "CONSTRAINED-STORAGE-001",
  "TIME-001",
]
inactive_claim_ids = ["API-SURFACE-001"]
disposition = "This corrective evidence is the current active time/generation proof."

[[record]]
source = "docs/evidence/WP-100-deadline-cleanup-timing.toml"
verification_command = "tools/check-wp100-deadline-cleanup-timing.sh"
active_claim_ids = [
  "CLEANUP-RECORD-001",
  "HANDLER-CANCEL-001",
  "HANDLER-CANCEL-002",
  "TIME-001",
]
inactive_claim_ids = ["API-SURFACE-001"]
disposition = "Reaffirm deadline and cleanup semantics; the retired umbrella surface id carries no gate authority."

[[record]]
source = "docs/evidence/WP-100-handler-context.toml"
verification_command = "tools/check-wp100-handler-context.sh"
active_claim_ids = ["HANDLER-API-001"]
inactive_claim_ids = ["API-SURFACE-001"]
disposition = "Reaffirm the active context boundary; the retired umbrella surface id carries no gate authority."

[[record]]
source = "docs/evidence/WP-100-property-read-handler-slice.toml"
verification_command = "tools/check-wp100-property-read-handler-slice.sh"
active_claim_ids = [
  "API-PAYLOAD-001",
  "CLEANUP-RECORD-001",
  "HANDLER-API-001",
  "HANDLER-VALUE-001",
]
inactive_claim_ids = []
disposition = "All claims remain active and are reaffirmed without changing implementation or fixture scope."

[[record]]
source = "docs/evidence/WP-200-property-read-plan-slice.toml"
verification_command = "tools/check-wp200-property-read-plan-slice.sh"
active_claim_ids = [
  "DOC-RUNTIME-001",
  "FORM-FINALIZE-001",
  "FORM-OWNER-001",
  "PLAN-ARTIFACT-001",
  "PLAN-COST-001",
  "PLAN-SET-001",
]
inactive_claim_ids = []
disposition = "All narrow Property Read plan-slice claims remain active; broad WP-200 and cross-package completion remain separate evidence."
