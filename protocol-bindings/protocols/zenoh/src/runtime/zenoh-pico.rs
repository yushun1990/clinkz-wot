//! Reserved runtime backend for constrained `zenoh-pico` integrations.
//!
//! The `runtime-zenoh-pico` feature intentionally fails at the crate root until
//! the C ABI, platform I/O, memory, and polling boundaries are implemented.
