# Project State

This file is a compact, non-authoritative continuation cache. It owns the last
observed remote basis, established execution frontier, current blocker or
handoff, stopping point, conditional next actions, and a small navigation set.
Replace stale content; keep this file at or below 200 lines.

## Continuation Projection

Observed default branch: `0347a418ffc47299df14d6c613f1ab0f80aa63c8`

Observed on: 2026-08-14 after fetching `origin/master`; verifying PR #29's
actual `master` base `65cbd4d93f100145595b4a6c8f8961b3b1d9b947`, exact head
`5148edc1d0b32bde3561949b3287593b23a37cb3`, merge ancestry, expected
21-path content, merge-tree equality, and successful merge-revision `mainline`
run `31714427530`.

Projection mode: conditional remote handoff

### Before verified integration

`EXECUTION.md` revision 1 is `PLANNED` on
`agent/zenoh-property-read-feedback-probe` in draft PR #30. Stop for the
required independent ChatGPT plan challenge. High may not begin until Max
records and resolves that challenge. The claim changes no product API: it
first tests whether a real compiler artifact can reach the unchanged target
route entry without a substitute. A concrete unconstructible boundary returns
`BLOCKED`; it does not authorize an inline SPI correction.

### After verified integration

Fetch `master` and verify the pull request's actual base, exact reviewed head,
merge ancestry, expected content, and merge-revision validation. If fresh Max
accepted a complete probe, a later Max cycle disposes every concrete finding
before considering the aggregate Property Read candidate. If an independently
reviewed blocker record is integrated, the later cycle selects only the exact
affected SPI correction. Neither path begins automatically.

## Established Frontier

- Active architecture authority is v5.0 bounded-core.
- PR #29 is integrated at
  `0347a418ffc47299df14d6c613f1ab0f80aa63c8`; its merge tree equals reviewed
  head `5148edc1d0b32bde3561949b3287593b23a37cb3`, and default-branch run
  `31714427530` passed.
- D48 transition-validator convergence and all six narrow Property Read
  tranches are integrated and validated.
- `PROPERTY-READ-ARCHITECTURE` remains `ready`, not `passed`; neither planned
  aggregate fixture root exists.
- The required real-target Zenoh feedback probe precedes aggregate mock
  candidate/admission and grants no WP-600 progress.

## Current Execution Contract

`EXECUTION.md` revision 1 plans one non-authoritative, external-author,
real-loopback Zenoh Property Read feedback probe at the exact observed default
basis. Its success boundary includes real plan/artifact provenance, network
query/reply, readiness, failure, cancellation/drain, cleanup, and multiple
Thing/route/form shape. Its current stopping point is independent plan
challenge, not execution.

## Limits and Blockers

- Source inspection finds that current host/static route preparation receives
  a `PrepareInput` containing only `BindingArtifactRef`, while the typed
  artifact payload remains in the Servient-owned plan set; no public payload
  resolution path was found. This is a named probe falsifier, not yet an
  authority change.
- The narrow host owner consumes one configuration on its first `produce_td`
  installation. The required multi-Thing/route probe must test this boundary
  rather than weaken it or use multiple isolated fixtures as a substitute.
- Detailed Host/static containers and lifecycle carriers remain provisional;
  the macro ownership, boundedness, generation, permit, no-hidden-dispatch,
  and cleanup-transfer invariants remain authoritative.
- Zenoh and zenoh-pico are one protocol family; this claim cannot establish
  general protocol-shape neutrality.
- The original `agent/property-read-architecture-candidate` checkout retains
  pre-existing uncommitted aggregate preparation. Preserve it unchanged; it is
  out of scope and superseded in execution order by this probe.

## Stopping Point

The Max planning task ends after publishing this exact plan on one draft pull
request. Wait for an independent Plan Challenger. Do not implement the probe,
repair a suspected carrier, or resume aggregate fixture work in this context.

## Navigation

- Current claim and frozen acceptance boundary: `EXECUTION.md`
- Roadmap and milestone frontier: `PLAN.md`
- Role lifecycle and challenge workflow: `PROJECT_GOVERNANCE.md`
- Architecture maturity boundary: `ARCHITECTURE_GOVERNANCE.md`
- Probe decision: `workspace/0056-target-spi-external-validation-and-cross-protocol-neutrality.md`
- Target SPI authority: `docs/spec/binding-spi.md`
- Property Read gate: `docs/work-packages/PROPERTY-READ-ARCHITECTURE.md`
- Exact gate state: `docs/work-packages/property-read-architecture-gate.toml`
