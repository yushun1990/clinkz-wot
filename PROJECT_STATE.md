# Project State

This file is a compact, non-authoritative continuation cache. It owns the last
observed remote basis, established execution frontier, current blocker or
handoff, stopping point, conditional next actions, and a small navigation set.
It does not retain decision history, commit chains, detailed specifications,
review evidence, or implementation status already owned elsewhere. Replace
stale content; keep this file at or below 200 lines.

## Continuation Projection

Observed default branch: `65cbd4d93f100145595b4a6c8f8961b3b1d9b947`

Observed on: 2026-08-13 after fetching `origin/master`, verifying PR #28's
actual base and exact head, merge ancestry, expected ten-path content, merge
tree equivalence, and successful default-branch `mainline` run `31708054337`.

Projection mode: conditional remote handoff

### Before verified integration

The bounded task is the execution-model and evidence-order reset on branch
`agent/execution-model-reset`. Finish only its governance, roadmap, workspace,
state, specification, and lightweight checker changes; validate them; publish
one draft pull request; obtain exact-head acceptance and remote validation;
then integrate and verify the merge revision. Do not begin a product candidate,
aggregate fixture, or real Zenoh probe in this task.

### After verified integration

Fetch `master` and verify the reset's actual merge ancestry, expected content,
and merge-revision `validation`. `EXECUTION.md` is intentionally `IDLE`; stop
with no active product claim. A later explicit progression request starts a
fresh Max planning context. That context may select the roadmap's real-target
Zenoh feedback probe, but must first replace `EXECUTION.md` with one bounded
claim, plan, and acceptance contract. No roadmap item authorizes execution by
itself.

## Established Frontier

- Active architecture authority is v5.0 bounded-core.
- PR #27 is integrated at `cc88a0c455eb8d4bb2d48a2efc7ced6a7b6a594c`;
  default-branch run `31705003835` passed.
- D48 transition-validator convergence is integrated through PR #28 merge
  `65cbd4d93f100145595b4a6c8f8961b3b1d9b947`; run `31708054337` passed.
- All six narrow Property Read tranches are `complete`/`approved`.
- `PROPERTY-READ-ARCHITECTURE` remains `ready`, not `passed`; neither planned
  aggregate fixture root exists.
- Repository evidence now places a non-authoritative real-target Zenoh
  feedback probe before aggregate mock-gate candidate admission.

## Current Execution Contract

`EXECUTION.md` is `IDLE`. This governance reset is the bootstrap boundary for
the new role-separated process, not an authorization to start its first product
claim. The next contract will be authored by Max from a freshly fetched and
reconciled default branch.

## Limits and Blockers

- Detailed target Binding/Servient lifecycle carriers remain provisional until
  real protocol feedback; frozen ownership, boundedness, generation, permit,
  no-hidden-dispatch, and cleanup-transfer invariants remain authoritative.
- Zenoh and zenoh-pico are one protocol family. Without a materially different
  protocol-shape fixture, the empirical release claim remains narrower than
  general protocol neutrality.
- The original `agent/property-read-architecture-candidate` checkout contains
  pre-existing uncommitted aggregate preparation. Preserve it as
  non-authoritative investigation evidence only. It is superseded in order by
  the real-target probe and may not be committed or resumed without a new
  `EXECUTION.md` contract.

## Stopping Point

After the execution-model reset is verified on the default branch, stop. The
next session begins with Max planning; High implementation, fresh Max
acceptance, optional ChatGPT plan challenge, and periodic Ultra audit follow
the new governance.

## Navigation

- Roadmap and milestone frontier: `PLAN.md`
- Current claim template and lifecycle: `EXECUTION.md`
- Execution roles and review workflow: `PROJECT_GOVERNANCE.md`
- Architecture authority and reopening: `ARCHITECTURE_GOVERNANCE.md`
- Evidence-order decision: `workspace/0056-target-spi-external-validation-and-cross-protocol-neutrality.md`
- Exact Property Read gate state: `docs/work-packages/property-read-architecture-gate.toml`
- Work-package DAG and evidence: `docs/work-packages/index.toml`
